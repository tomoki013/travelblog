import styles from "./ScrollIcon.module.css";

const ScrollIcon = () => {
    return (
        <div className={styles.scroll_icon}>
            <span>Scroll</span>
        </div>
    );
};

export default ScrollIcon;
